# ── Web framework ─────────────────────────────────────────────────
Flask==3.1.3
Flask-Login==0.6.3
flask-cors==6.0.5
Werkzeug==3.1.8
python-dotenv==1.2.2

# ── HTTP ──────────────────────────────────────────────────────────
requests==2.33.1
requests-oauthlib==2.0.0

# ── Data ──────────────────────────────────────────────────────────
pandas==3.0.2
numpy==2.4.4
openpyxl>=3.1

# ── Google Sheets ─────────────────────────────────────────────────
gspread==6.2.1
google-auth==2.49.1
google-auth-oauthlib==1.3.1

# ── Production server ─────────────────────────────────────────────
gunicorn==26.0.0

# ── PostgreSQL (Render) ────────────────────────────────────────────
psycopg2-binary>=2.9

# ── Multi-user sync queueing (prevents concurrent imports from
#    clashing / hammering the Google Sheets API at the same time) ──
filelock>=3.16.0

# REMOVED: selenium, webdriver-manager (replaced by Chrome extension)
# REMOVED: schedule (not imported anywhere - scheduler.py/scheduler_service.py use plain time.sleep loops)

# ── Transitive deps pinned for reproducible installs ───────────────
attrs==26.1.0
blinker==1.9.0
certifi==2026.2.25
cffi==2.0.0
charset-normalizer==3.4.6
click==8.3.2
colorama==0.4.6
cryptography==46.0.6
h11==0.16.0
idna==3.11
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
oauthlib==3.3.1
packaging==26.1
pyasn1==0.6.3
pyasn1_modules==0.4.2
pycparser==3.0
python-dateutil==2.9.0.post0
six==1.17.0
sortedcontainers==2.4.0
typing_extensions==4.15.0
tzdata==2026.1
urllib3==2.6.3
