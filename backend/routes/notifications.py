"""
routes/notifications.py — placeholder.

The original app.py had no notification center — flash messages were the
only feedback mechanism. This file is scaffolded here, empty, so Phase 3
has a home for a real notification inbox (mentor assignments, sync
failures, weekly report ready, etc), backed by the `notifications` table
added in Phase 1's migration.
"""

from extensions import app  # noqa: F401
